var options = {
    strings: ['student', 'Web developer', 'web designer'],
    typeSpeed: 70,
    startDelay: 500,
    backSpeed: 40,
    backDelay: 1000,
    loop: true,
    loopCount: Infinity
};
let typed = new Typed('.element2', options);